define(['skeleton', 'config'],
function(sk, config) {

    var Workbench = sk.Model.extend({
        configure: function(){

        },
        emptyFn: function(){}
    });
    return Workbench;
});